# TODO

- [ ] `Runtime/Infrastructure.meta` 빈 meta 파일 정리
  - 현상: Unity 에디터에서 `Packages/com.redmins.red-core-framework/Runtime/Infrastructure` 폴더가 없다는 경고 발생. 원격 레포에 빈 폴더의 meta 파일만 남아있음.
  - 해결: `Runtime/Infrastructure.meta` 삭제하고 커밋/푸시 (또는 Infrastructure 폴더에 실제 파일 추가).
  - 영향: 경고일 뿐 동작에는 문제 없음.
